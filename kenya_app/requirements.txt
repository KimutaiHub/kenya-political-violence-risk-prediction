# Requirements for kenya_app (the deployed Streamlit intelligence dashboard).
#
# Deliberately pinned tighter than a typical requirements.txt - see
# README.md, "Model version compatibility". The model .joblib files are
# fitted under numpy<2 / scikit-learn<1.4 so that Python 3.8 environments
# (which cannot install numpy>=2.0 at all) can load them, and so that
# newer scikit-learn's internal changes (e.g. added DecisionTreeClassifier
# attributes) don't break prediction at runtime even after a successful
# load. Install from this file exactly - don't let pip resolve looser
# versions here even if your Python version would technically allow it.
#
# This is a SEPARATE file from requirements-notebook.txt (used to run the
# capstone notebook itself, where newer versions are fine).

streamlit>=1.29,<1.36
pandas>=2.0,<2.2
numpy<2
scikit-learn>=1.3,<1.4
xgboost>=2.0,<2.1
plotly>=5.22
pyarrow>=15.0
joblib>=1.4
requests>=2.31
shap>=0.44,<0.46
feedparser>=6.0

# pipeline/download_acled.py - Aggregated Africa workbook download + parsing
beautifulsoup4>=4.12
openpyxl>=3.1.2
